## The Algorithm — v0.0.11 (Banner Stub — Amendment S level)

**This file is a banner stub. It carries no substantive doctrine.**

v0.0.11 names the Amendment S ratification level (Part 16, operator-ratified 2026-07-19). Per that ratification's own "no version fork" policy (F→R precedent), the operative doctrine TEXT remains the consolidated `v0.0.10.md` — Parts 1–16, highest-version-wins. This stub exists so `LATEST` and the runtime version metadata (`version.json.algorithm`, `DOS-Install/version.json.algorithm`) can name the ratified level while `algorithm-version-contract.test.ts` stays mechanical: `v${algorithm}` equals `LATEST` and `<LATEST>.md` exists.

---

## §1 — Operative spec

Read `~/.claude/DOS/Algorithm/v0.0.10.md` IN FULL — Parts 1–16 inclusive; Part 16 (Amendment S) is ACTIVE. Overlay map: `INDEX.md`.

**Flip provenance:** cc#302 (2026-07-19) ratified Part 16 and bumped the metadata; runtime PR #306 harmonized metadata back to the then-current pointer and added the version contract; this commit completes the two-surface flip (LATEST + both metadata files → v0.0.11) as one deliberate change under the operator ruling of 2026-07-20 (session 3b121edc, PRD 20260720-032909 D8).
